import { View } from "moti";
import { styled } from 'styled-components';

export const StyledAnimatedView = styled(View)`
  justify-content: center;
  width: fit-content;
  margin: 18px;
  padding: 10px;
  border-radius: 15px;
`;